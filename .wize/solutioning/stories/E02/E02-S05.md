---
story_id: E02-S05
epic: E02
status: ready-for-dev
priority: 1
estimate: S
linked_acs: [AC-E02-5]
---

# Story: Add JSON Schema for project-scan-report state file

## Context

The state file must be validated so resume never crashes on malformed JSON.

## Acceptance criteria

- **AC-E02-5:** A JSON Schema at `src/method-skills/1-analysis/wize-document-project/templates/project-scan-report-schema.json` exists.
- Schema requires: `workflow_version`, `timestamps`, `mode`, `scan_level`, `completed_steps`, `current_step`.
- Schema supports optional `findings`, `outputs_generated`, `resume_instructions`, `validation_status`, `deep_dive_targets`.
- `npm run validate` includes the schema in structural checks.

## Notes for Shuri

- Copy/adapt BMAD schema; change `project_knowledge` path semantics to Wize (`document-project`).
- Add schema file to validators test.

## Notes for Hawkeye

- Test valid and invalid state files against the schema.
