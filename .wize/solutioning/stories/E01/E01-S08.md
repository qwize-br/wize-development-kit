---
story_id: E01-S08
epic: E01
status: done
priority: 2
estimate: M
linked_acs: [AC-E01-8]
---

# Story: Generate master index.md with incomplete markers

## Context

BMAD uses `index.md` as the primary AI retrieval entry point, with `_(To be generated)_` markers for docs that should exist but weren't produced.

## Acceptance criteria

- **AC-E01-8:** `index.md` is generated in `.wize/knowledge/document-project/` listing all generated docs and existing docs.
- Conditional docs that were skipped get `_(To be generated)_` marker.
- Index structure adapts: simple for single-part, part-based for multi-part.
- After missing docs are later generated, re-running index generation removes their markers.

## Out of scope

- Interactive prompt to fill missing docs (can be future).

## Notes for Shuri

- Module path suggestion: `tools/installer/document-project/render-index.js`.
- Accept a list of generated file paths and existing docs; produce markdown.
- Use relative links from `.wize/knowledge/document-project/`.

## Notes for Hawkeye

- Test single-part and multi-part index shapes.
- Test marker removal after a missing doc is added.
